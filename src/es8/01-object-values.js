const countries = {
    MX: "Mexico",
    CO: "Colombia",
    CL: "Chile",
    PE: "Peru",
}
console.log(Object.values(countries)); // solo los valores(value)
console.log(Object.keys(countries)); // solo las llaves(key)